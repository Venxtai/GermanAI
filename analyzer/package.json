{
  "name": "impuls-deutsch-text-analyzer",
  "version": "1.0.0",
  "description": "Impuls Deutsch Text Analyzer — curriculum-aware text analysis for German language teachers",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1",
    "@anthropic-ai/sdk": "^0.39.0",
    "googleapis": "^140.0.0",
    "pdf-parse": "^1.1.1",
    "mammoth": "^1.8.0",
    "pdfkit": "^0.15.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
