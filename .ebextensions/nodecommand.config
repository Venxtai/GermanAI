option_settings:
  aws:elasticbeanstalk:application:environment:
    NPM_USE_PRODUCTION: false
  # Raise ALB idle timeout so SSE log connections aren't killed at 60s
  aws:elb:policies:
    ConnectionSettingIdleTimeout: 3600
